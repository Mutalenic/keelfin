<% content_for(:page_title) { "Edit Recurring Transaction" } %>
<% content_for(:skip_card_wrapper, true) %>

<div class="space-y-8">
  <!-- Hero Banner -->
  <div class="rounded-3xl p-6 sm:p-8 text-white shadow-[0_25px_80px_rgba(15,23,42,0.45)] border border-white/10"
       style="background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 55%, #1e1b4b 100%);">
    <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-6">
      <div>
        <p class="text-xs uppercase tracking-[0.3em] text-white/70 mb-3">Recurring</p>
        <h1 class="text-2xl sm:text-3xl font-semibold tracking-tight">Edit Transaction</h1>
        <p class="mt-2 text-sm sm:text-base text-white/80 max-w-2xl">
          Update recurring payment or income details.
        </p>
      </div>
      <div class="flex items-center gap-3">
        <%= link_to recurring_transaction_path(@recurring_transaction), class: "inline-flex items-center px-5 py-3 rounded-2xl text-sm font-semibold border border-white/30 text-white/90 hover:bg-white/5 transition" do %>
          <i class="fa-solid fa-arrow-left mr-2"></i>
          Back to Transaction
        <% end %>
      </div>
    </div>
  </div>

  <div class="max-w-3xl mx-auto">
    <div class="bg-white/95 border border-white/30 backdrop-blur-2xl rounded-3xl shadow-2xl p-6 sm:p-8">
      <%= render 'form', recurring_transaction: @recurring_transaction %>
    </div>
  </div>
</div>
