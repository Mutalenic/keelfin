<div class="bg-surface border border-default backdrop-blur-2xl rounded-3xl shadow-2xl shadow-black/20 p-6">
  <div class="flex justify-between items-center mb-4">
    <h2 class="text-xl font-semibold text-content">Budget vs Actual Spending</h2>
    <div class="flex space-x-2">
      <button id="view-as-chart" class="bg-content/5 text-subtle px-3 py-1 rounded-xl hover:bg-content/10 border border-default">
        <i class="fa-solid fa-chart-bar mr-1"></i> Chart
      </button>
      <button id="view-as-table" class="bg-content/5 text-subtle px-3 py-1 rounded-xl hover:bg-content/10 border border-default">
        <i class="fa-solid fa-table mr-1"></i> Table
      </button>
    </div>
  </div>

  <div id="budget-chart" class="mb-6">
    <!-- Chart will be rendered here -->
    <div class="h-64">
      <canvas id="budget-vs-actual-chart"></canvas>
    </div>
  </div>

  <script>
    // Make budget vs actual data available to the charts.js script
    const budgetVsActualData = {
      categories: <%= raw json_escape(@budget_categories.to_json) %>,
      budgetAmounts: <%= raw json_escape(@budget_amounts.to_json) %>,
      actualAmounts: <%= raw json_escape(@actual_amounts.to_json) %>
    };
  </script>

  <div id="budget-table" class="hidden">
    <% if @budget_categories.any? %>
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-default">
          <thead class="bg-content/5">
            <tr>
              <th class="px-6 py-3 text-left text-xs font-medium text-subtle uppercase tracking-wider">Category</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-subtle uppercase tracking-wider">Budget</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-subtle uppercase tracking-wider">Actual</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-subtle uppercase tracking-wider">Variance</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-default">
            <% @budget_categories.each_with_index do |category, index| %>
              <tr class="hover:bg-content/10">
                <td class="px-6 py-4 whitespace-nowrap">
                  <div class="text-sm font-medium text-content"><%= category %></div>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <div class="text-sm font-medium text-content">K<%= number_with_precision(@budget_amounts[index], precision: 2) %></div>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <div class="text-sm font-medium text-content">K<%= number_with_precision(@actual_amounts[index], precision: 2) %></div>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <% variance = @actual_amounts[index] - @budget_amounts[index] %>
                  <div class="text-sm font-medium <%= variance > 0 ? 'text-red-400' : 'text-emerald-400' %>">
                    <%= variance > 0 ? '+' : '' %>K<%= number_with_precision(variance, precision: 2) %>
                    (<%= number_with_precision((variance / @budget_amounts[index] * 100).abs, precision: 1) %>% <%= variance > 0 ? 'over' : 'under' %>)
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <% else %>
      <p class="text-accent text-center py-4">No budget data available</p>
    <% end %>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const chartButton = document.getElementById('view-as-chart');
    const tableButton = document.getElementById('view-as-table');
    const chartDiv = document.getElementById('budget-chart');
    const tableDiv = document.getElementById('budget-table');
    
    if (chartButton && tableButton && chartDiv && tableDiv) {
      chartButton.addEventListener('click', function() {
        chartDiv.classList.remove('hidden');
        tableDiv.classList.add('hidden');
        chartButton.classList.remove('bg-content/5', 'text-subtle', 'border-default');
        chartButton.classList.add('bg-brand-600', 'text-white', 'border-brand-500');
        tableButton.classList.remove('bg-brand-600', 'text-white', 'border-brand-500');
        tableButton.classList.add('bg-content/5', 'text-subtle', 'border-default');
      });

      tableButton.addEventListener('click', function() {
        chartDiv.classList.add('hidden');
        tableDiv.classList.remove('hidden');
        chartButton.classList.remove('bg-brand-600', 'text-white', 'border-brand-500');
        chartButton.classList.add('bg-content/5', 'text-subtle', 'border-default');
        tableButton.classList.remove('bg-content/5', 'text-subtle', 'border-default');
        tableButton.classList.add('bg-brand-600', 'text-white', 'border-brand-500');
      });
    }
  });
</script>
