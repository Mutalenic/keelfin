<div class="container mx-auto px-4 py-8">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold">Budget Management</h1>
    <%= link_to "Create Budget", new_budget_path, class: "bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700" %>
  </div>
  
  <!-- BNNB Comparison -->
  <% if @bnnb_comparison %>
    <div class="bg-green-50 border border-green-200 rounded-lg p-6 mb-6">
      <h2 class="text-xl font-semibold mb-4">📊 JCTR Benchmark Comparison</h2>
      <p class="text-sm text-gray-600 mb-4">Compare your budgets to Zambian national averages</p>
      <div class="grid grid-cols-2 gap-4">
        <div>
          <p class="text-sm text-gray-600">JCTR Food Basket</p>
          <p class="text-xl font-bold">K<%= number_with_precision(@bnnb_comparison[:bnnb_food] || 0, precision: 2) %></p>
        </div>
        <div>
          <p class="text-sm text-gray-600">Your Food Spending</p>
          <p class="text-xl font-bold">K<%= number_with_precision(@bnnb_comparison[:user_food] || 0, precision: 2) %></p>
        </div>
      </div>
      <p class="text-xs text-gray-500 mt-4">Source: JCTR BNNB January 2026</p>
    </div>
  <% end %>
  
  <!-- Budgets List -->
  <div class="bg-white rounded-lg shadow p-6">
    <h2 class="text-xl font-semibold mb-4">Your Budgets</h2>
    
    <% if @budgets.any? %>
      <div class="space-y-4">
        <% @budgets.each do |budget| %>
          <div class="border border-gray-200 rounded-lg p-4">
            <div class="flex justify-between items-start mb-3">
              <div>
                <h3 class="font-semibold text-lg"><%= budget.category.name %></h3>
                <p class="text-sm text-gray-600">
                  Monthly Limit: K<%= number_with_precision(budget.monthly_limit, precision: 2) %>
                  <% if budget.inflation_adjusted %>
                    <span class="ml-2 px-2 py-1 bg-blue-100 text-blue-800 text-xs rounded">Inflation Adjusted</span>
                  <% end %>
                </p>
              </div>
              <div class="flex gap-2">
                <%= link_to "Edit", edit_budget_path(budget), class: "text-blue-600 hover:text-blue-800 text-sm" %>
                <%= link_to "Delete", budget_path(budget), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "text-red-600 hover:text-red-800 text-sm" %>
              </div>
            </div>
            
            <div class="mb-2">
              <div class="flex justify-between text-sm mb-1">
                <span>Spent: K<%= number_with_precision(budget.current_spending, precision: 2) %></span>
                <span class="<%= budget.is_overspent? ? 'text-red-600 font-semibold' : 'text-gray-600' %>">
                  <%= number_with_precision(budget.percentage_used, precision: 1) %>%
                </span>
              </div>
              <div class="w-full bg-gray-200 rounded-full h-2">
                <div class="<%= budget.is_overspent? ? 'bg-red-600' : 'bg-green-600' %> h-2 rounded-full" 
                     style="width: <%= [budget.percentage_used, 100].min %>%"></div>
              </div>
            </div>
            
            <% if budget.is_overspent? %>
              <p class="text-sm text-red-600 mt-2">⚠️ Over budget by K<%= number_with_precision(-budget.remaining_budget, precision: 2) %></p>
            <% else %>
              <p class="text-sm text-green-600 mt-2">✅ K<%= number_with_precision(budget.remaining_budget, precision: 2) %> remaining</p>
            <% end %>
          </div>
        <% end %>
      </div>
    <% else %>
      <p class="text-gray-500 text-center py-8">No budgets created yet. Click "Create Budget" to start managing your spending limits.</p>
    <% end %>
  </div>
  
  <div class="mt-6 bg-yellow-50 border border-yellow-200 rounded-lg p-4">
    <h3 class="font-semibold mb-2">💡 Budgeting Tips for Zambia</h3>
    <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
      <li>Enable inflation adjustment to automatically update budgets based on Zambia's 9.4% inflation rate</li>
      <li>Compare your food budget to JCTR's K4,900 benchmark for a family of five</li>
      <li>Track essential vs. discretionary spending to identify savings opportunities</li>
      <li>Set realistic budgets based on JCTR BNNB data to avoid month-end shortfalls</li>
    </ul>
  </div>
  
  <div class="mt-6">
    <%= link_to "← Back to Dashboard", dashboard_path, class: "text-blue-600 hover:text-blue-800" %>
  </div>
</div>
