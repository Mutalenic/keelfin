<% content_for(:page_title) { "New Category" } %>
<% content_for(:skip_card_wrapper, true) %>

<div class="space-y-8">
  <!-- Hero Banner -->
  <div class="rounded-3xl p-6 sm:p-8 text-white shadow-[0_25px_80px_rgba(2,44,34,0.45)] border border-brand-400/20"
       style="background: linear-gradient(130deg, #022c22 0%, #065f46 55%, #059669 100%);">
    <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-6">
      <div>
        <p class="text-xs uppercase tracking-[0.3em] text-white/70 mb-3">Money map</p>
        <h1 class="text-2xl sm:text-3xl font-semibold tracking-tight">Add new category</h1>
        <p class="mt-2 text-sm sm:text-base text-white/80 max-w-2xl">
          Create custom categories or choose from presets to organize your spending.
        </p>
      </div>
      <div class="flex items-center gap-3">
        <%= link_to categories_path, class: "inline-flex items-center px-5 py-3 rounded-2xl text-sm font-semibold border border-white/30 text-white/90 hover:bg-white/5 transition" do %>
          <i class="fa-solid fa-arrow-left mr-2"></i>
          Back to categories
        <% end %>
      </div>
    </div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
    <!-- Custom Category Form -->
    <div class="bg-brand-900/80 border border-brand-700/40 backdrop-blur-2xl rounded-3xl shadow-2xl shadow-black/20 p-6 sm:p-8">
      <h2 class="text-xl font-bold mb-6 text-brand-100">Create Custom Category</h2>

      <%= form_with(model: @category, url: categories_path, method: :post, class: "space-y-5") do |form| %>
        <% if @category.errors.any? %>
          <div class="bg-red-900/30 border border-red-700/40 rounded-2xl p-4 mb-4">
            <h2 class="text-red-300 font-semibold mb-2"><%= pluralize(@category.errors.count, "error") %> prohibited this from being saved:</h2>
            <ul class="text-red-300 list-disc list-inside text-sm">
              <% @category.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div>
          <%= form.label :name, class: "block text-sm font-medium text-brand-300 mb-2" %>
          <%= form.text_field :name, class: "w-full px-4 py-3 bg-brand-800/60 border border-brand-700/50 rounded-xl focus:ring-2 focus:ring-brand-500 focus:border-brand-500 text-sm text-brand-100 placeholder-brand-500/50", placeholder: 'Enter category name' %>
        </div>

        <div>
          <%= form.label :description, class: "block text-sm font-medium text-brand-300 mb-2" %>
          <%= form.text_area :description, class: "w-full px-4 py-3 bg-brand-800/60 border border-brand-700/50 rounded-xl focus:ring-2 focus:ring-brand-500 focus:border-brand-500 text-sm text-brand-100 placeholder-brand-500/50", placeholder: 'Enter category description', rows: 3 %>
        </div>

        <div>
          <%= form.label :icon_name, class: "block text-sm font-medium text-brand-300 mb-2" %>
          <%= form.select :icon_name,
            [
              ['Apple', 'fa-apple-whole'],
              ['Home', 'fa-home'],
              ['Car', 'fa-car'],
              ['Shopping', 'fa-shopping-cart'],
              ['Food', 'fa-utensils'],
              ['Health', 'fa-hospital'],
              ['Education', 'fa-graduation-cap'],
              ['Entertainment', 'fa-film'],
              ['Bills', 'fa-file-invoice-dollar'],
              ['Savings', 'fa-piggy-bank']
            ],
            { include_blank: 'Select an icon' },
            { class: "w-full px-4 py-3 bg-brand-800/60 border border-brand-700/50 rounded-xl focus:ring-2 focus:ring-brand-500 focus:border-brand-500 text-sm text-brand-100" }
          %>
        </div>

        <div>
          <%= form.label :color, class: "block text-sm font-medium text-brand-300 mb-2" %>
          <%= form.color_field :color, class: "w-full h-12 p-1 bg-brand-800/60 border border-brand-700/50 rounded-xl focus:ring-2 focus:ring-brand-500 focus:border-brand-500" %>
        </div>

        <div>
          <%= form.label :category_type, class: "block text-sm font-medium text-brand-300 mb-2" %>
          <%= form.select :category_type,
            @category_types.map { |type| [type.capitalize, type] },
            { include_blank: 'Select a type' },
            { class: "w-full px-4 py-3 bg-brand-800/60 border border-brand-700/50 rounded-xl focus:ring-2 focus:ring-brand-500 focus:border-brand-500 text-sm text-brand-100" }
          %>
        </div>

        <div class="pt-2">
          <%= form.submit 'Create Category', class: "w-full inline-flex justify-center items-center px-6 py-3 rounded-xl text-sm font-semibold bg-brand-600 text-white shadow hover:bg-brand-500 transition cursor-pointer" %>
        </div>
      <% end %>
    </div>

    <!-- Preset Categories -->
    <div class="bg-brand-900/80 border border-brand-700/40 backdrop-blur-2xl rounded-3xl shadow-2xl shadow-black/20 p-6 sm:p-8">
      <h2 class="text-xl font-bold mb-2 text-brand-100">Quick Add Presets</h2>
      <p class="text-brand-300 mb-6">Choose from predefined categories to get started quickly.</p>

      <div class="space-y-3">
        <% @preset_categories.each do |preset| %>
          <div class="flex items-center justify-between p-4 rounded-2xl border border-brand-700/30 hover:shadow-md transition bg-brand-800/50" style="border-left: 4px solid <%= preset[:color] %>">
            <div class="flex items-center">
              <div class="w-10 h-10 rounded-xl flex items-center justify-center text-white mr-3" style="background-color: <%= preset[:color] %>">
                <i class="<%= preset[:icon_name] %>"></i>
              </div>
              <div>
                <h3 class="font-semibold text-brand-100"><%= preset[:name] %></h3>
                <p class="text-xs text-brand-400"><%= preset[:category_type].capitalize %></p>
              </div>
            </div>
            <%= button_to 'Add', add_preset_categories_path(preset_name: preset[:name]), class: "px-3 py-1.5 rounded-lg text-sm font-semibold bg-brand-800 hover:bg-brand-700 text-brand-300 transition border border-brand-700/50" %>
          </div>
        <% end %}
      </div>
    </div>
  </div>
</div>
